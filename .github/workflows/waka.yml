name: Waka Readme

on:

  schedule:

    # Runs every 4 hours

    - cron: '0 */4 * * *'

  workflow_dispatch:

jobs:

  update-readme:

    name: Update Readme with Metrics

    runs-on: ubuntu-latest

    steps:

      - uses: anmol098/waka-readme-stats@master

        with:

          WAKATIME_API_KEY: ${{ secrets.2f6ecc74-d05c-4538-ab52-60528104d851 }}

          SHOW_OS: "True"

          SHOW_PROJECTS: "True"

          SHOW_PROFILE_VIEWS: "True"

          SHOW_EDITORS: "False"

          SHOW_LANGUAGE_PER_REPO: "True"

          SHOW_LOC_CHART: "True"

          SHOW_LINES_OF_CODE: "True"

          SHOW_SHORT_INFO: "True"